using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraControl : MonoBehaviour
{
    //public PlayerMovement playerMovement;
    //public Transform playerTransform;
    //public Vector3 offset;
    //public float speed;
    //void Start()
    //{
    //    speed = 5f;
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    //offset = new Vector3(playerTransform.position.x, playerTransform.position.y, -20f);
    //    offset = new Vector3(playerMovement.lastPosition.x, playerMovement.lastPosition.y, -20f);

    //    transform.position = Vector3.MoveTowards(offset, transform.position, speed * Time.deltaTime);
    //}
}
